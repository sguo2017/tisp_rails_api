<%- model_class = User -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.new', '新增%{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>

<%= render 'users/form_new', user: @target_user %>

<%= link_to t('.back', :default => t("helpers.links.back")), admin_users_path, :class => 'btn btn-default' %>
