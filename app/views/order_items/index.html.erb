<%- model_class = OrderItem -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:deal_id) %></th>
      <th><%= model_class.human_attribute_name(:serv_offer_id) %></th>
      <th><%= model_class.human_attribute_name(:offer_user_id) %></th>
      <th><%= model_class.human_attribute_name(:request_user_id) %></th>
      <th><%= model_class.human_attribute_name(:lately_chat_content) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @order_items.each do |order_item| %>
      <tr>
        <td><%= link_to order_item.id, order_item_path(order_item) %></td>
        <td><%= order_item.deal_id %></td>
        <td><%= order_item.serv_offer_id %></td>
        <td><%= order_item.offer_user_id %></td>
        <td><%= order_item.request_user_id %></td>
        <td><%= order_item.lately_chat_content %></td>
        <td><%=l order_item.created_at %></td>
        <td>
          <%= link_to t('.show', :default => t("helpers.links.show")),
                      order_item_path(order_item), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_order_item_path(order_item), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      order_item_path(order_item),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @order_items %>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_order_item_path,
            :class => 'btn btn-primary' %>
