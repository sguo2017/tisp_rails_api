<%= form_for(serv_offer) do |f| %>
  <% if serv_offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(serv_offer.errors.count, "error") %> prohibited this serv_offer from being saved:</h2>

      <ul>
      <% serv_offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :serv_title %>
    <%= f.text_field :serv_title %>
  </div>

  <div class="field">
    <%= f.label :serv_detail %>
    <%= f.text_field :serv_detail %>
  </div>

  <div class="field">
    <%= f.label :serv_images %>
    <%= f.text_field :serv_images %>
  </div>

  <div class="field">
    <%= f.label :serv_catagory %>
    <%= f.text_field :serv_catagory %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
